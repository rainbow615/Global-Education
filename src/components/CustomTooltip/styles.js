import styled from 'styled-components'
import { Tag } from 'antd'

export const CopyLabel = styled(Tag)({
  textTransform: 'uppercase',
  display: 'flex',
  alignContent: 'center',
  userSelect: 'none',
  fontSize: '0.65rem',
  padding: '0.125rem',
  fontFamily: 'InterMedium',
})
